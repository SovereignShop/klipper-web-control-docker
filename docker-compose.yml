

services:

  klipper:
    image: dimalo/klipper-moonraker
    build:
      dockerfile: ./klipper/Dockerfile
      context: .
      # args:
      #   DEVICE_GROUP: device
      #   DEVICE_GID: 987
    container_name: klipper
    ports:
      - 7125:7125
    restart: unless-stopped
    volumes:
      - gcode_files:/home/klippy/gcode_files
      # be aware to create your own branch if you mount the config folder as it will be updated on the main branch
      # that way you can merge upstream changes to your customized configs
      - ./config:/home/klippy/.config
      - moonraker_data:/home/klippy/.moonraker
      # - <<your_config_path>>:/home/klippy/.config
      - ./kossel_1_printer.cfg:/home/klippy/.config/printer.cfg
    # mount serial device - take care to grant sufficient permissions to the device: <host_dev>:<container_dev>
    # put <container_dev> into your printer.cfg
    devices:
      - /dev/serial/by-id/usb-Klipper_stm32f103xe_33FFD6055641343442671843-if00:/dev/serial/by-id/usb-Klipper_stm32f103xe_33FFD6055641343442671843-if00

  ##################################################################
  # Example config for a second printer on a different serial port
  ##################################################################

  kossel_2:
    image: dimalo/klipper-moonraker
    build:
      dockerfile: ./klipper/Dockerfile
      context: .
    container_name: kossel_2
    ports:
      - 7126:7125
    restart: unless-stopped
    volumes:
      - gcode_files:/home/klippy/gcode_files
      - ./config:/home/klippy/.config
      - moonraker_data:/home/klippy/.moonraker
      - ./kossel_2_printer.cfg:/home/klippy/.config/printer.cfg
    devices:
      - /dev/serial/by-id/usb-Klipper_stm32f103xe_38FFDB055357303903721043-if00:/dev/serial/by-id/usb-Klipper_stm32f103xe_38FFDB055357303903721043-if00

  kossel_3:
    image: dimalo/klipper-moonraker
    build:
      dockerfile: ./klipper/Dockerfile
      context: .
    container_name: kossel_3
    ports:
      - 7127:7125
    restart: unless-stopped
    volumes:
      - gcode_files:/home/klippy/gcode_files
      - ./config:/home/klippy/.config
      - moonraker_data:/home/klippy/.moonraker
      - ./kossel_3_printer.cfg:/home/klippy/.config/printer.cfg
    devices:
      - /dev/serial/by-id/usb-Klipper_stm32f103xe_33FFDA055641343406612043-if00:/dev/serial/by-id/usb-Klipper_stm32f103xe_33FFDA055641343406612043-if00

  kossel_4:
    image: dimalo/klipper-moonraker
    build:
      dockerfile: ./klipper/Dockerfile
      context: .
    container_name: kossel_4
    ports:
      - 7128:7125
    restart: unless-stopped
    volumes:
      - gcode_files:/home/klippy/gcode_files
      - ./config:/home/klippy/.config
      - moonraker_data:/home/klippy/.moonraker
      - ./kossel_4_printer.cfg:/home/klippy/.config/printer.cfg
    devices:
      - /dev/serial/by-id/usb-Klipper_stm32f103xe_33FFD6054252353117882157-if00:/dev/serial/by-id/usb-Klipper_stm32f103xe_33FFD6054252353117882157-if00

  fluidd:
    image: cadriel/fluidd
    # build:
    #   dockerfile: Dockerfile
    #   context: frontend
    container_name: fluidd
    ports:
      - 8010:80

  mainsail:
    image: dimalo/mainsail
    build:
      dockerfile: Dockerfile
      context: frontend
      args:
        FRONTEND_ZIP_URL: https://github.com/meteyou/mainsail/releases/latest/download/mainsail.zip
    container_name: mainsail
    ports:
      - 8011:80
    depends_on: 
      - klipper
      # - klipper_another_printer
    links:
      - klipper:klipper
      # - klipper_another_printer:klipper_another_printer

volumes: 
  gcode_files:
  moonraker_data:

